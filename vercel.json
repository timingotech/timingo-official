{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "functions": {
    "api/jobs.js": { "maxDuration": 30 }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
